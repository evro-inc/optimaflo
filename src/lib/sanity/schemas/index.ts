import project from './project-schema';
import index from './index-schema';
import aboutPage from './about-schema';
import FeaturesPage from './features-schema';

const schemas = [project, index, aboutPage, FeaturesPage];

export default schemas;
